;; Default shell on bottom

;; want two windows at startup
; (split-window-vertically)

;; move to other window
; (other-window 1)

;; start a shell
; (shell)

;; move back to first window
; (other-window 1)


;; Default only one window
(delete-other-windows)
