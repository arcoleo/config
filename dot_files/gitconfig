[user]
	name = David Arcoleo
	email = darcoleo@splunk.com

[alias]
    st = status
    co = checkout
    ci = commit
    b = branch
    bd = branch --delete
    nbr = checkout -b
    p = push
    l = pull
    bp = "!sh -c 'git branch $1 && git push -u origin $1' -"
    bpc = "!sh -c 'git branch $1 && git push -u origin $1 && git checkout $1' -"
    bdr = "!sh -c 'git branch --delete $1 && git push origin --delete $1' -"

[color]
    ui = true

[push]
    default = simple
	followTags = true

[core]
    pager = less -FRSX
	excludesfile = /Users/darcoleo/.gitignore_global

[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
	path = 

[mergetool "sourcetree"]
	cmd = /Applications/Sourcetree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true

[commit]
	template = /Users/darcoleo/.stCommitMsg
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f

# [url "https://darcoleo:QiC6VX1oi7NuMA92r7eM@cd.splunkdev.com/cloudworks"]
# 	insteadOf = http://cd.splunkdev.com/cloudworks

[url "git@cd.splunkdev.com:"]
    insteadOf = https://cd.splunkdev.com/
